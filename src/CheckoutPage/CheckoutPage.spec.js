const { render, fireEvent } = require("@testing-library/react");
const { CheckoutPage } = require("./CheckoutPage");

describe("CheckoutPage", () => {
  describe("Form renders correctly", () => {
    it.todo("renders correctly");
    it.todo("Page shows order data");
  });
});
